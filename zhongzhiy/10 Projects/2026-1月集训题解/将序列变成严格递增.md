# 将序列 $A$ 变成严格递增需要最少修改几个数
为了保证严格递增，必须满足 $A_j - A_i \ge j - i$（对于 $j > i$）。

我们将公式变形：

$$A_j - j \ge A_i - i$$

令新序列 **$B_i = A_i - i$**。

**结论：**

原序列 $A$ 中能够保留不动的元素，在序列 $B$ 中必须满足 **非递减**（$B_i \le B_j$）。

因此，**最多保留的元素个数 = 序列 $B$ 的最长非递减子序列的长度**。