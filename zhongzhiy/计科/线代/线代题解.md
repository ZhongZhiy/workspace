x是n维列向量，$x  x^T=1$,令$H = E-2x  x^T$,求证H是对称正交矩阵


$$\begin{array}
 2-\alpha  & 2   & -2  \\
2  &  5-\alpha  & -4 \\
-2 & -4 & 5-\alpha
\end{array}$$


矩阵$A=\left( \begin{matrix}2 & 0 & 1 \\ 3  & 1 & x \\ 4 & 0 & 5\end{matrix} \right )$可相似对角化，求x


x 设A为n阶方阵，且$|A|=1$或$|A|=-1$，则A为正交矩阵

x 设p为方阵A的对于特征值$\lambda$的特征向量，则kp（k为任意常数）也是A的特征向量 

x A与B有相同的特征值则A，B相似
**如果矩阵 A 和 B 相似**，那么它们一定有**相同的特征值**（重数也一样）

> 因为：
- 矩阵是否相似，不光要特征值一样，
    
- 还要它们的**特征子空间结构**（比如每个特征值对应的特征向量数目，几何重数、代数重数）也要一样！
    

>比如：

- 特征值的重数（代数重数）要一样，
    
- 特征向量的数量（几何重数）也要一样，
    
- 最好矩阵能被对角化（有完整的特征向量组）。



x 设A为实对称矩阵，且存在可逆矩阵P使$P^{-1}AP=\Lambda$,则P必是正交矩阵

设A，B为实对称矩阵，且A与B相似，则A，B合同


设A是10阶对称矩阵，2是A的特征方程的3重根，则$R(A-2E)=$
A对应于特征值2的线性无关的特征向量的个数是



喵～这个说法是**正确的**喵！来详细告诉你为啥喵！

---



$$
\boxed{\text{AB = 0} \quad \text{且} \quad A,B\neq 0 \quad \Longrightarrow \quad \text{A的行向量或B的列向量线性相关}}
$$
