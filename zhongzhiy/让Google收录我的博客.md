我的blog是使用`hexo`生成的, 
1. 在 Google Search Console 中选择右边的填入网址([ZhongZhiy's blog!](https://zhongzhiy.github.io/)), 
2. 下载 Google 的验证 html文件, 
3. 放入 `/blog/source/`文件夹下, 
4. 在`_config.yml` 添加 `skip_render = '*.html`禁止渲染这个文件, 
5. 过一会, 验证即可
之后就可以直接通过bing的验证

