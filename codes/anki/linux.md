# Linux
## 大量用户会妨碍什么？
大量用户会妨碍未来的发展。
## 软件设计的一个重要长远原则是什么？
避免被大量用户束缚而妨碍未来发展。
## X程序员展示的开发原则是什么？
在开发重要且希望长期存在的产品时，应在开始阶段投入大量时间设计一个灵活且深思熟虑的结构。
## 一个好的软件设计能带来什么长期效果？
使产品拥有持久的生命力。
## 使用容易学习的工具的最终感觉是什么？
系统由计算机控制。
## 使用功能强大但需花时间学习的工具的最终感觉是什么？
系统由用户控制。
## Unix 系统属于哪种类型的工具？
设计良好、功能强大但需要时间学习的工具。
## 切换终端的快捷键是什么？
Alt + Ctrl + F1~F7。
## 切换账户但不切换环境的命令是？
su username。
## 切换账户并切换环境的命令是？
su - username。
## 修改程序设置的方式是什么？
修改其配置文件。
## 使用哪个命令可以阅读文档？
less。
## less 启动后按哪个键向前翻页？
<Space>。
## less 向后翻页的键是？
b。
## less 查看帮助使用哪个键？
h。
## less 退出使用哪个键？
q。
## 文件 /boot/grub/menu.lst 的作用是什么？
包含关于计算机上可引导的操作系统的信息。
## 文件 /etc/hosts 的作用是什么？
存放系统已知的主机名和IP地址。
## 文件 /etc/inittab 的作用是什么？
定义不同的运行级别。
## 文件 /etc/passwd 的作用是什么？
保存每个用户标识的基本信息（实际口令加密保存在其他地方）。
## 文件 /etc/profile 的作用是什么？
用户登录时自动执行的命令文件。
## 文件 /etc/samba/smb.conf 的作用是什么？
Samba 的配置文件，用于 Unix 与 Windows 共享文件和打印机。
## 运行级别0表示什么？
关机。
## 运行级别6表示什么？
重启。
## 修改运行级别的命令？
init。
## 命令 sudo init 6 的效果？
重启系统。
## reboot 命令的作用？
重启系统。
## shutdown now 命令的作用？
立即关机。
## 用户id（userid）是什么？
系统为每个用户分配的唯一标识。
## 显示用户信息的命令？
w 或 who。

---
# part 2

## 显示当前用户标识的命令？
whoami。
## 查看上一次登录时间的命令？
last。
## 超级用户的提示符号是什么？
#。
## 在 /etc/inittab 中修改哪个字段可设置默认运行级别？
initdefault。
## 运行级别1表示什么？
单用户命令行模式。
## 运行级别3表示什么？
多用户命令行模式。
## 运行级别5表示什么？
多用户图形界面模式。
## 使用哪个组合键切换虚拟控制台？
Ctrl + Alt + F1~F6。
## 图形界面通常对应哪个虚拟控制台？
第7个（Ctrl + Alt + F7）。
## 在X窗口中，点击中键的功能是什么？
粘贴选中的内容。
## 在X窗口中，双击选中什么？
单词。
## 在X窗口中，三击选中什么？
整行。
## 命令 su 的全称是什么？
substitute userid。
## su mmuser 命令的作用？
切换为指定用户mmuser。
## su - mmuser 命令的作用？
切换为mmuser用户并加载其环境。
## id 命令的作用？
显示当前用户的userid与组信息。
## 终端的符号是什么？
tty。
## 查看当前终端类型的命令？
echo $TERM。

## 键盘中 ^C 被映射到什么？
intr（中断命令）。
## ^W 的作用？
删除单词。
## ^U 的作用？
删除整行。
## backspace 映射到哪个字符？
^H。
## ^\ 的作用？
发送 stop 信号，类似于 intr。
## ^S 的作用？
暂停程序。
## ^Q 的作用？
继续程序。
## ^D 的作用？
发送EOF信号。
## 命令 stty -a 的作用？
显示所有终端设置。
## 修改按键映射的命令？
stty。
## 修改擦除键映射的命令示例？
stty erase ^H。
## {{c1::返回字符}} = {{c2::^M}}。
## {{c1::换行字符}} = {{c2::^J}}。
## 每行文本必须以一个 {{c1::新行字符 (^J)}} 结束。
## 按下 <Return> 键时，发送 {{c1::^M}}，Unix 自动将其转换为 {{c2::^J}}。
## Unix 在显示数据时，会将 {{c1::^J}} 改为 {{c2::^M^J}}。
## which 命令的作用？
显示程序的路径。
## type 命令的作用？
判断命令是内部命令还是外部命令。
## 退出程序常用的快捷方式？
q、quit、^D、^C。
## bc 程序的功能？
命令行计算器，支持函数编程。
## 查看日历命令？
cal 9 2025。
## uptime 命令的作用？
显示计算机运行时间。
## uname 命令的作用？
显示系统名称。
## hostname 命令的作用？
显示计算机主机名。
## whoami 命令的作用？
显示当前用户。
## w 命令的作用？
显示当前在线用户及其活动。
## who 命令的作用？
显示当前登录的用户列表。
## lock 命令的作用？
临时锁定终端。
## leave 命令的作用？
设置提醒离开时间。
## echo 命令的作用？
将输入内容合并为一行输出。
## man 命令的作用？
查看命令的联机手册。
## man 页面按哪个键查看帮助？
h。
## man 页面执行 shell 命令的方式？
!command，例如 !date。
## man 手册的简写查询命令？
man -f 或 whatis。
## man 手册搜索关键字命令？
man -k 或 apropos。
## info 命令的作用？
查看GNU格式的帮助信息。
## info 下一页快捷键？
Space 或 pd。
## info 上一页快捷键？
Backspace 或 pu。
## 命令结构由哪三部分组成？
命令名称、选项、参数。
## shell 中变量的设置格式？
NAME=value。
## export 命令的作用？
将变量设置为环境变量。
## unset 命令的作用？
取消变量定义。
## 显示所有环境变量的命令？
env 或 printenv。
## 在bash中，0代表什么？
标准输入。
## 在bash中，1代表什么？
标准输出。
## 在bash中，2代表什么？
标准错误。
## 重定向到同一文件的语法？
command 1>output 2>&1。
## 子shell会继承父shell的什么？
环境变量。
## 子shell中的修改是否保留到父shell？
不会。
## 抛弃输出的方式？
重定向到 /dev/null。
## 管道的作用？
将一个程序的输出作为另一个程序的输入。
## tee 命令的作用？
将输出同时写入文件和标准输出。
## tee -a 的作用？
追加输出到文件。

## 使用 && 表示什么？
前一命令成功后执行下一命令。
## 使用 || 表示什么？
前一命令失败后执行下一命令。
## 文件 /dev/tty 表示什么？
当前终端设备。
## 文件 /dev/null 的作用？
丢弃所有写入的数据（数据黑洞）。
## 文件 /dev/zero 的作用？
持续输出零字节。
## 文件 /dev/random 的作用？
输出随机数据。
## 文件 /dev/urandom 的作用？
输出伪随机数据。
## 命名管道的创建命令？
mkfifo pipename。
## 命名管道的作用？
在进程间传递数据（进程间通信）。
## proc 文件系统的作用？
提供访问内核和进程信息的接口。
## mount 命令的作用？
将文件系统挂载到目录树。
## umount 命令的作用？
卸载文件系统。
## /bin 目录存放什么？
二进制文件（命令和工具）。
## /boot 目录存放什么？
引导文件和内核。
## /dev 目录存放什么？
设备文件（包括物理与伪设备）。
## /etc 目录存放什么？
系统配置文件。
## /home 目录存放什么？
用户个人目录。
## /lib 目录存放什么？
系统程序运行所需的库。
## /media 目录的作用？
可移动介质挂载点。
## /mnt 目录的作用？
临时挂载点。
## /opt 目录的作用？
第三方软件安装位置。
## /root 目录的作用？
超级用户（root）的主目录。
## /sbin 目录的作用？
系统级二进制程序。
## /tmp 目录的作用？
临时文件。
## /usr 目录存放什么？
辅助系统文件、程序与静态数据。
## /var 目录存放什么？
可变数据（如日志、缓存）。
## /usr/local 目录的作用？
本地安装的程序与资料。
## /usr/share 目录的作用？
共享的静态资源，如文档、字体、图标。
## pushd 命令的作用？
将目录压入目录栈并切换到该目录。
## popd 命令的作用？
从目录栈中弹出并切换回上一个目录。
## dirs 命令的作用？
显示目录栈内容。
## dirs -v 的作用？
显示带编号的目录栈。
## dirs -c 的作用？
清空目录栈。
## alias d="dirs -v" 的作用？
简化显示目录栈的命令。
## ls -C | wc -l 的作用？
统计文件个数。
## ls -r 的作用？
反向排序输出。
## ls -R 的作用？
递归显示目录内容。
## ls -F 的作用？
在输出中显示文件类型标识。
## ls -d 的作用？
仅显示目录本身。
## ls -u 的作用？
按访问时间显示。
## ls -t 的作用？
按修改时间排序。
## file 命令的作用？
检测文件类型。
## ls -s 的作用？
显示文件大小。
## ls -h 的作用？
以人类可读格式显示大小。
## du filename 的作用？
显示文件所占磁盘空间。
## df -h 的作用？
显示磁盘使用情况。
## 磁盘最小分配单元通常是多少？
4KB。
## 文件类型符号“-”表示什么？
普通文件。
## 文件类型符号“d”表示什么？
目录。
## 文件类型符号“l”表示什么？
符号链接。
## 文件类型符号“b”表示什么？
块设备文件。
## 文件类型符号“c”表示什么？
字符设备文件。
## 文件类型符号“p”表示什么？
命名管道。
## touch 命令的作用？
修改文件的访问时间和修改时间。
## touch -m 的作用？
只修改修改时间。
## touch -a 的作用？
只修改访问时间。
## touch -t 200812231030 file 的作用？
设置文件时间为指定时间。
## touch -c 的作用？
不创建不存在的文件。
## cp file1 file2 的作用？
将文件1复制为文件2。
## cp -r 的作用？
递归复制目录。
## mv 命令的作用？
移动或重命名文件。
## mv -i 的作用？
移动前交互询问。
## mv -f 的作用？
强制移动不询问。
## rm -i 的作用？
删除前交互询问。
## rm -f 的作用？
强制删除。
## rm -r 的作用？
递归删除目录。
## 在删除文件前应执行什么命令？
ls 检查目标。
## 文件读权限的值是多少？
4。
## 文件写权限的值是多少？
2。
## 文件执行权限的值是多少？
1。
## chmod 命令的作用？
修改文件权限。
## 新建不可执行文件的默认权限是多少？
666。
## 新建可执行文件或目录的默认权限是多少？
777。
## umask 的作用？
定义创建文件时应减去的默认权限。
## umask 022 的效果？
默认权限去除组和其他用户的写权限。
## setuid 权限的作用？
允许普通用户以文件属主权限运行程序。
## 查看文件 inode 信息的命令？
stat file。
## 查看文件 inode 编号的命令？
ls -i。
## 硬链接的创建命令？
ln file newname。
## 符号链接的创建命令？
ln -s file newname。
## 硬链接和符号链接的区别？
硬链接共享相同 inode；符号链接指向路径。
## whereis 命令的作用？
查找命令的可执行文件、手册或源代码。
## whereis -b 的作用？
仅查找可执行文件。
## whereis -m 的作用？
仅查找手册文件。
## locate 命令的作用？
通过数据库快速查找文件。
## locate -r '*.jpg$' 的作用？
查找所有 jpg 结尾的文件。
## locate -c 的作用？
只统计匹配数量。
## locate -i 的作用？
忽略大小写。
## find 命令的基本格式？
find path test action。
## find . -name '*.png' 的作用？
在当前目录查找所有 png 文件。
## find 的 -exec 参数的作用？
对每个匹配结果执行指定命令。
## xargs 命令的作用？
将输入转换为命令参数批量执行。
## find . -name '*.png' | xargs echo 的作用？
将所有 png 文件名传递给 echo 输出。
## xargs -i 的作用？
插入占位符 {} 多次使用输入。
## xargs -p 的作用？
执行前提示确认。
## xargs -t 的作用？
显示生成的命令。
## find . -type f | xargs -i mv {} {}.old 的作用？
批量重命名文件，添加 .old 后缀。
## 进程表由谁维护？
内核调度器。
## 系统调用的作用？
让进程请求内核执行服务。
## 常见系统调用有哪些？
fork、exec、wait、exit。
## 输出当前 shell 的 PID 命令？
echo $$。
## fork 调用返回给父进程和子进程的是什么？
不同的返回值，用于区分。
## 僵尸进程是什么？
已结束但仍在进程表中等待回收的进程。
## 孤儿进程是什么？
父进程先于子进程结束的进程。
## init 进程的作用？
所有进程的祖先进程，收养孤儿进程。
## 在命令末尾加 & 的作用？
在后台运行程序。
## 后台程序不能做的两件事？
不能读取键盘输入；不能输出到终端。
## sleep 命令的作用？
延迟执行。
## 发送挂起信号的快捷键？
^Z。
## 恢复挂起作业的命令？
fg。
## 显示当前作业列表的命令？
jobs。
## 将作业移到后台运行的命令？
bg。
## ps 命令的作用？
查看系统进程。
## ps -ef 的作用？
显示完整进程信息（UNIX格式）。
## ps aux 的作用？
显示所有进程信息（BSD格式）。
## top 命令的作用？
动态查看系统进程与资源使用。
## pstree 命令的作用？
以树形结构显示进程关系。
## kill 命令的作用？
发送信号终止进程。
## kill -9 的作用？
强制终止进程。
## kill -l 的作用？
列出所有信号。
## nice 命令的作用？
以指定优先级运行程序。
## renice 命令的作用？
调整正在运行进程的优先级。
## nice 值越大代表什么？
优先级越低。
## 守护进程是什么？
在后台运行、与终端无关的服务程序。
## daemon 进程的特点？
无控制终端、自动运行、常驻内存。
## 过滤器的定义是什么？
从标准输入读取数据并向标准输出写出数据的程序。
## cat 命令的作用？
将数据原样输出到标准输出。
## cat > file 的作用？
创建一个新文件并输入内容。
## cat >> file 的作用？
向文件追加内容。
## cat < file 的作用？
显示文件内容。
## cat file1 file2 的作用？
连接多个文件并输出。
## cat -n 的作用？
显示行号。
## cat -b 的作用？
为非空行编号。
## cat -s 的作用？
压缩多余空行为一行。
## split 命令的作用？
将文件按行数或大小分割成多个小文件。
## split -l 5000 file 的作用？
将文件按每5000行拆分。
## split -d 的作用？
使用数字作为后缀。
## split -a 3 的作用？
后缀长度为3位。
## rev 命令的作用？
反转每行的字符顺序。
## tac 命令的作用？
反转整个文件的行顺序。
## head -n 的作用？
显示文件前n行。
## tail -n 的作用？
显示文件最后n行。
## colrm 的作用？
删除指定列范围的字符。
## cmp 命令的作用？
逐字节比较两个文件是否相同。
## comm 命令的作用？
比较两个排序后的文件，输出三列差异。
## comm -123 的含义？
抑制对应的第1、2、3列输出。
## grep 命令的作用？
搜索匹配文本行。
## grep 的输入来自哪里？
标准输入或指定文件。
## grep 支持哪种模式？
正则表达式。
## grep -i 的作用？
忽略大小写匹配。
## grep -v 的作用？
输出不匹配的行。
## grep -n 的作用？
显示匹配行的行号。
## grep -r 的作用？
递归搜索目录。
## grep -E 的作用？
使用扩展正则表达式。
## vi 命令 `vim -R file` 的作用？
以只读模式打开文件。
## vi 命令 `view file` 的作用？
以只读模式查看文件。
## vim -r file 的作用？
恢复异常关闭时的文件。
## vi 保存并退出的快捷键？
ZZ。
## 在 vi 中，`~` 表示什么？
空行。
## ^I 在 vi 中表示什么？
制表符。
## ^W 在 vi 中的作用？
删除最后一个单词。
## ^U 在 vi 中的作用？
删除当前行。
## 0 在 vi 中的作用？
移动到行首。
## $ 在 vi 中的作用？
移动到行尾。
## ^ 在 vi 中的作用？
移动到行首第一个非空字符。
## w 在 vi 中的作用？
移动到下一个单词首。
## e 在 vi 中的作用？
移动到下一个单词尾。
## b 在 vi 中的作用？
移动到上一个单词首。
## ( 在 vi 中的作用？
移动到上一个句子。
## ) 在 vi 中的作用？
移动到下一个句子。
## { 在 vi 中的作用？
移动到上一个段落。
## } 在 vi 中的作用？
移动到下一个段落。
## H 在 vi 中的作用？
移动到屏幕顶部。
## M 在 vi 中的作用？
移动到屏幕中部。
## L 在 vi 中的作用？
移动到屏幕底部。
## ^F 的作用？
下移一屏。
## ^B 的作用？
上移一屏。
## ^D 的作用？
下移半屏。
## ^U 的作用？
上移半屏。
## vi 命令 ``（双反引号）的作用？
跳转回上次光标所在行。
## ma 在 vi 中的作用？
标记当前位置为 a。
## 'a 的作用？
跳转到标记 a 的行首。
## /word 在 vi 中的作用？
向下搜索单词 word。
## ?word 的作用？
向上搜索单词 word。
## n 的作用？
重复上一次搜索。
## N 的作用？
反向重复上一次搜索。
## :12 的作用？
跳转到第12行。
## r 的作用？
替换单个字符。
## R 的作用？
替换多个字符。
## s 的作用？
删除一个字符并进入插入模式。
## C 的作用？
从光标处到行尾替换。
## S 的作用？
替换整行。
## cw 的作用？
替换一个单词。
## :s/old/new/ 的作用？
将当前行第一个 old 替换为 new。
## :s/old/new/g 的作用？
替换当前行所有匹配的 old。
## :s/old/new/c 的作用？
替换时逐个确认。
## :12,55s/old/new/ 的作用？
在第12至55行间替换。
## %s/old/new/g 的作用？
全文件替换。
## x 的作用？
删除光标处字符。
## X 的作用？
删除光标前字符。
## D 的作用？
删除从光标到行尾。
## dw 的作用？
删除一个单词。
## db 的作用？
删除前一个单词。
## dd 的作用？
删除整行。
## dG 的作用？
删除从当前行到文件末尾。
## dgg 的作用？
删除从当前行到文件开头。
## :12d 的作用？
删除第12行。
## u 的作用？
撤销上一步操作。
## U 的作用？
恢复当前行。
## . 的作用？
重复上一次命令。
## 1p 的作用？
粘贴上一次删除的内容到下一行。
## P 的作用？
粘贴到上一行。
## y 命令的作用？
复制（yank）文本。
## ~ 的作用？
切换字符大小写。
## :set 命令的作用？
查看或修改编辑选项。
## :set option? 的作用？
查看某个选项的值。
## :set wrapmargin=6 的作用？
设置自动换行边距为6。
## :set autoindent 的作用？
启用自动缩进。
## J 的作用？
合并当前行与下一行。
## 3J 的作用？
合并当前行与后3行。
## :.,$co12 的作用？
复制当前行到文件第12行后。
## :.,$m12 的作用？
移动当前行到第12行。
## :!date 的作用？
执行外部命令 date。
## :!! 的作用？
重复上次外部命令。
## :12r file 的作用？
在第12行读取文件内容插入。
## :12r !command 的作用？
将命令输出插入到文件中。
## n!!fmt 的作用？
用 fmt 命令格式化当前行。
## :w >> file 的作用？
将缓冲区内容追加到文件。
## :e file 的作用？
编辑另一个文件。
## :ab eg egg and god 的作用？
创建缩写 eg → egg and god。
## :una eg 的作用？
删除缩写 eg。
## :map x command 的作用？
创建宏，将按键x映射为命令。
## :unmap x 的作用？
删除宏映射。
## mkdir 命令创建了什么？
新目录和对应的 inode。
## cp 覆盖文件时 inode 会变化吗？
不会，除非创建新文件。
## mv 命令会改变 inode 吗？
不会。
## ln file newname 创建的是什么类型链接？
硬链接。
## ln -s dir link 创建的是什么？
符号链接。
## ls -l 中如何区分符号链接？
显示“->”箭头。
## shred 命令的作用？
粉碎文件，防止恢复。
## find . -name '*.png' 2>/dev/null 的作用？
忽略错误信息输出。
## echo $$ 的输出是什么？
当前shell的PID。
## bg 的作用？
将挂起作业放到后台继续运行。
## jobs -l 的作用？
显示作业及其PID。
## kill -9 PID 的作用？
强制终止进程。
## nice -n 10 command 的作用？
以较低优先级运行命令。
## renice -5 -p PID 的作用？
提高进程优先级。
## 守护进程的典型用途？
提供后台服务（如网络、日志、计划任务等）。
## find 命令的结构是什么？
find 路径 测试条件 操作。
## find . -type f 的作用？
查找当前目录下所有普通文件。
## find . -name '*.sh' 的作用？
查找所有扩展名为 .sh 的文件。
## find /etc -type d 的作用？
查找 /etc 下的所有目录。
## find / -user root 的作用？
查找属主为 root 的文件。
## find -size +100M 的作用？
查找大于100MB的文件。
## find -mtime -1 的作用？
查找最近1天内修改过的文件。
## find -perm 755 的作用？
查找权限为755的文件。
## find -exec command {} \; 的作用？
对每个查找到的文件执行命令。
## find . -name '*.log' -exec rm {} \; 的作用？
删除所有 .log 文件。
## find . -name '*.png' | xargs ls 的作用？
批量列出所有 .png 文件信息。
## find 中 {} 的含义？
占位符，代表当前匹配的文件。
## xargs 命令的作用？
把标准输入转为命令参数执行。
## xargs -i 的作用？
使用 {} 占位符多次引用输入。
## xargs -p 的作用？
每次执行命令前询问确认。
## xargs -t 的作用？
显示生成的命令。
## xargs -n 1 的作用？
每次传递一个参数执行命令。
## find . -type f | xargs -i mv {} {}.bak 的作用？
批量重命名文件，添加 .bak 后缀。
## locate 命令使用的数据库路径？
/var/lib/mlocate/mlocate.db。
## updatedb 命令的作用？
更新 locate 使用的数据库。
## whereis -b ls 的作用？
查找 ls 的可执行文件位置。
## whereis -m ls 的作用？
查找 ls 的手册文件位置。
## whereis -s ls 的作用？
查找 ls 的源代码文件位置。
## echo /etc/shells 的作用？
列出系统安装的所有 shell。
## echo $SHELL 的作用？
显示当前使用的 shell。
## 暂时更换 shell 的方法？
直接输入 shell 名称（如 sh、bash、zsh）。
## 永久更换登录 shell 的命令？
chsh。
## shell 变量与环境变量的区别？
shell 变量是局部的；环境变量可被子进程继承。
## 在 bash 中设置变量的命令？
NAME=value。
## 将变量设置为环境变量的命令？
export NAME。
## 同时创建并导出变量的命令？
export NAME=value。
## 取消变量的命令？
unset NAME。
## 查看所有 shell 变量的命令？
set。
## 显示所有环境变量的命令？
env 或 printenv。
## HOME 变量表示什么？
用户的主目录。
## HOSTNAME 变量表示什么？
计算机名称。
## PATH 变量表示什么？
系统搜索可执行文件的路径。
## PWD 变量表示什么？
当前工作目录。
## TERM 变量表示什么？
终端类型。
## USER 变量表示什么？
当前登录用户。
## PATH 变量的修改命令？
export PATH="$PATH:/new/path"。
## 将目录添加到 PATH 末尾的命令？
export PATH="$PATH:$HOME/bin"。
## shell 提示符变量名？
PS1。
## 修改提示符为显示用户名的命令？
PS1="${USER}$ "。
## 查看命令类型（内部或外部）的命令？
type command。
## 查看内部命令帮助的命令？
help command。
## 外部命令的搜索顺序？
按 PATH 中的目录顺序查找。
## 使用反引号的作用？
执行命令并将结果替换到命令中。
## 命令 `basename $SHELL` 的作用？
获取当前 shell 的名称。
## 查看命令历史的命令？
history。
## 调用历史命令 !num 的作用？
执行编号为 num 的命令。
## 使用 fc -s num 的作用？
执行历史编号为 num 的命令。
## 替换历史命令中字符串的格式？
!number:s/old/new/。
## 历史列表大小由哪个变量控制？
HISTSIZE。
## 创建命令别名的命令？
alias name=command。
## 解除别名的命令？
unalias name。
## 查看所有别名的命令？
alias。
## 临时忽略别名运行命令的方法？
在命令前加反斜杠 \。
## 登录 shell 启动时会执行哪些文件？
登录文件与环境文件。
## bash 的登录文件？
.profile。
## bash 的环境文件？
.bashrc。
## bash 的注销文件？
.bash_logout。
## csh 的登录文件？
.login。
## csh 的环境文件？
.cshrc 或 .tcshrc。
## csh 的注销文件？
.logout。
## 登录文件的作用？
设置环境和初始化工作会话。
## 环境文件的作用？
设置shell选项、别名和函数等。
## 重定向符号 > 的作用？
将输出重定向到文件（覆盖）。
## >> 的作用？
将输出追加到文件末尾。
## noclobber 选项的作用？
防止输出重定向覆盖已有文件。
## >| 的作用？
即使启用 noclobber 也强制覆盖。
## 0< inputfile 的作用？
将文件作为标准输入。
## 1> outputfile 的作用？
将标准输出写入文件。
## 2> errorfile 的作用？
将标准错误输出到文件。
## 同时重定向标准输出和标准错误到同一文件？
command 1>out 2>&1。
## 子shell 是什么？
由当前 shell 派生出的子进程。
## 子shell 的变量变化会影响父shell 吗？
不会。
## 在子shell中执行命令的方式？
用括号括起命令 (command)。
## 抛弃命令输出的方式？
重定向到 /dev/null。
## command >/dev/null 2>&1 的作用？
丢弃标准输出和错误输出。
## 管道的作用？
将一个程序的输出作为另一个程序的输入。
## 将标准错误也通过管道传输的写法？
command1 2>&1 | command2。
## tee 命令的作用？
将输出同时写入文件和标准输出。
## tee -a 的作用？
追加输出而不覆盖。
## command1 | tee file | command2 的作用？
将输出保存到文件并继续传给 command2。
## 条件执行运算符 && 的作用？
前一个命令成功时执行下一个命令。
## 条件执行运算符 || 的作用？
前一个命令失败时执行下一个命令。
## 命令管道中的分流工具？
tee。
## echo $? 的作用？
显示上一个命令的退出状态码。
## exit 0 表示什么？
正常退出。
## exit 1 表示什么？
执行失败。
## which 命令的作用？
显示命令的绝对路径。
## type -a 命令的作用？
显示命令的所有匹配定义。
## echo $PATH 的作用？
显示当前的命令搜索路径。
## source file 的作用？
在当前 shell 中执行文件内容。
## . file 的作用？
等同于 source file。
## shell 中 PATH 的默认分隔符？
冒号 :。
## 创建一个临时环境变量的方法？
在命令前添加变量定义：NAME=value command。
## export LANG=en_US.UTF-8 的作用？
设置系统语言环境。
## alias ls='ls --color=auto' 的作用？
让 ls 输出带颜色。
## history -c 的作用？
清除命令历史。
## !?pattern 的作用？
执行最近匹配该模式的命令。
## ^old^new 的作用？
在上一个命令中快速替换字符串并执行。
## jobs 命令显示的符号 + 表示什么？
当前作业。
## jobs 命令显示的符号 - 表示什么？
下一个作业。
## fg %1 的作用？
将作业1切换到前台。
## bg %1 的作用？
让作业1在后台继续运行。
## 文件权限中 r 表示什么？
读取权限。
## 文件权限中 w 表示什么？
写入权限。
## 文件权限中 x 表示什么？
执行权限。
## 对目录的读权限表示什么？
可以查看目录内容。
## 对目录的写权限表示什么？
可以在目录中创建、删除或重命名文件。
## 对目录的执行权限表示什么？
可以进入目录并访问其中文件。
## 使用 chmod 修改权限的命令格式？
chmod mode file。
## chmod 755 file 的含义？
所有者可读写执行，组和其他用户可读执行。
## chmod a+x file 的含义？
为所有用户添加执行权限。
## chmod go-w file 的含义？
移除组与其他用户的写权限。
## 文件权限数值 4 对应什么？
读权限。
## 文件权限数值 2 对应什么？
写权限。
## 文件权限数值 1 对应什么？
执行权限。
## 文件权限数值 0 对应什么？
无权限。
## umask 的作用是什么？
定义新建文件或目录的默认权限屏蔽位。
## umask 022 的含义？
新建文件时去掉组和其他用户的写权限。
## 文件权限 setuid 的作用？
执行文件时暂时以文件属主的权限运行。
## 执行权限中出现 s 表示什么？
setuid 或 setgid 权限。
## stat 命令的作用？
显示文件的详细状态与 inode 信息。
## inode 的作用？
存储文件的元信息（如权限、时间、大小等）。
## 使用 ls -i 可以查看什么？
文件的 inode 编号。
## 什么是硬链接？
多个文件名共享同一个 inode。
## 什么是符号链接？
指向目标文件路径的快捷方式。
## 创建硬链接的命令？
ln file linkname。
## 创建符号链接的命令？
ln -s file linkname。
## 删除原文件后，硬链接是否失效？
不会，数据仍可访问。
## 删除原文件后，符号链接是否失效？
会，变成悬空链接。
## ls -l 中符号链接文件显示什么符号？
->（箭头）。
## whereis 命令的作用？
查找命令的可执行文件、源文件及手册位置。
## locate 命令基于什么？
系统文件数据库。
## updatedb 命令的作用？
更新 locate 的数据库。
## find . -perm 644 的作用？
查找权限为 644 的文件。
## find . -exec rm {} \; 的作用？
删除当前目录及子目录中的所有匹配文件。
## find . -mtime +7 的作用？
查找7天前修改过的文件。
## ps 命令的作用？
查看进程状态。
## ps -ef 的输出格式？
UNIX 标准格式。
## ps aux 的输出格式？
BSD 格式。
## top 命令的作用？
动态显示进程和资源使用情况。
## pstree 命令的作用？
以树状结构显示进程之间的层级关系。
## kill 命令的作用？
发送信号以控制或终止进程。
## kill -9 PID 的作用？
发送 SIGKILL 信号强制终止进程。
## kill -l 的作用？
列出所有信号名称和编号。
## 常见的 SIGINT 信号编号？
2。
## 常见的 SIGTERM 信号编号？
15。
## 常见的 SIGKILL 信号编号？
9。
## SIGSTOP 信号的作用？
暂停进程（不可捕获）。
## SIGCONT 信号的作用？
继续被暂停的进程。
## 僵尸进程是什么？
已结束但未被父进程回收的进程。
## 孤儿进程是什么？
父进程结束后仍在运行的子进程。
## init 进程的作用？
系统的第一个进程，负责启动和管理其他进程。
## init 进程的 PID？
1。
## echo $$ 的输出是什么？
当前 shell 的 PID。
## 在命令后加 & 的作用？
让程序在后台运行。
## 后台程序不能做的两件事？
读取键盘输入；输出到终端。
## 挂起进程的快捷键？
^Z。
## 恢复挂起进程到前台的命令？
fg。
## 恢复挂起进程到后台的命令？
bg。
## jobs 命令的作用？
显示当前 shell 的作业列表。
## jobs -l 的作用？
显示作业列表及 PID。
## jobs 输出中的 “+” 表示什么？
当前作业。
## jobs 输出中的 “-” 表示什么？
前一个作业。
## fg %1 的作用？
将编号为1的作业切换到前台。
## bg %1 的作用？
将编号为1的作业放入后台运行。
## suspend 命令的作用？
挂起当前 shell。
## suspend -f 的作用？
强制挂起 shell。
## sleep 命令的作用？
让程序暂停指定时间。
## sleep 5 的作用？
暂停5秒。
## sleep 1m 的作用？
暂停1分钟。
## nice 命令的作用？
以指定优先级启动新进程。
## nice 值范围？
-20 到 +19。
## nice 值越大意味着什么？
进程优先级越低。
## renice 命令的作用？
修改正在运行进程的优先级。
## renice 10 -p 1234 的作用？
将 PID 1234 的进程优先级降低。
## ps -p PID 的作用？
查看指定进程信息。
## kill -STOP PID 的作用？
暂停进程。
## kill -CONT PID 的作用？
恢复暂停的进程。
## kill -HUP PID 的作用？
让进程重新加载配置文件。
## 守护进程（daemon）的定义？
后台运行、不依赖终端的进程。
## 守护进程的特点？
自动启动、常驻内存、提供系统服务。
## 守护进程常见举例？
cron、sshd、syslogd。
## 守护进程运行在哪个运行级别？
多用户模式（3或5）。
## systemctl start 服务名 的作用？
启动服务。
## systemctl stop 服务名 的作用？
停止服务。
## systemctl restart 服务名 的作用？
重启服务。
## systemctl enable 服务名 的作用？
设置开机自启。
## systemctl status 服务名 的作用？
查看服务状态。
## systemctl list-units --type=service 的作用？
列出系统中所有活动服务。
## journalctl 命令的作用？
查看系统日志。
## journalctl -u 服务名 的作用？
查看指定服务的日志。
## init 与 systemd 的区别？
systemd 是现代系统的初始化与服务管理框架，替代传统 init。
## systemd 的第一个进程 PID？
1。
## 守护进程与普通进程的主要区别？
守护进程没有控制终端。
## 如何将程序变为守护进程？
在后台运行并脱离终端控制。
## nohup 命令的作用？
忽略挂起信号，使程序在退出终端后继续运行。
## nohup command & 的作用？
后台持续运行命令，即使退出终端也不会结束。
## nohup 的默认输出文件？
nohup.out。
## disown 命令的作用？
将作业从当前 shell 的作业表中移除。
## 进程树的根是谁？
init 或 systemd。
## 僵尸进程的清除方式？
让父进程调用 wait()，或由 init 收养后清理。
## 查看僵尸进程的命令？
ps aux | grep Z。
## SIGCHLD 信号的作用？
子进程结束时通知父进程。
## exec 系统调用的作用？
用新程序替换当前进程映像。
## wait 系统调用的作用？
让父进程等待子进程结束。
## exit 系统调用的作用？
终止进程并返回状态码。
## fork 系统调用的作用？
创建一个子进程。
## ps -ef | grep bash 的作用？
查找 bash 进程。
## top -n 1 的作用？
仅显示一次系统状态后退出。
## top -p PID 的作用？
仅监视指定进程。
## pstree -p 的作用？
显示每个进程的PID。
## pstree -u 的作用？
显示每个进程的用户名。
## 终止多个作业的命令？
kill %1 %2 %3。
## killall 命令的作用？
终止所有指定名称的进程。
## killall -9 process 的作用？
强制终止所有同名进程。
## pgrep 命令的作用？
根据名称查找进程ID。
## pkill 命令的作用？
根据名称终止进程。
## jobs 命令是否显示后台守护进程？
否，只显示当前shell的作业。
## ps aux | grep service 的作用？
查找特定服务的进程。
## 进程优先级调整命令？
nice 与 renice。
## 后台进程输出重定向建议？
command > logfile 2>&1 &。
## grep 命令的作用？
在文本中搜索匹配的字符串。
## grep 的基本语法？
grep [选项] 模式 [文件]。
## grep -i 的作用？
忽略大小写匹配。
## grep -v 的作用？
显示不匹配的行。
## grep -n 的作用？
显示行号。
## grep -r 的作用？
递归搜索目录。
## grep -E 的作用？
使用扩展正则表达式。
## egrep 命令与 grep -E 有何关系？
等价。
## fgrep 命令的作用？
搜索固定字符串（不使用正则）。
## grep "pattern" file 的作用？
显示 file 中包含 pattern 的行。
## grep -c "pattern" file 的作用？
统计匹配行数。
## grep -l "pattern" * 的作用？
显示匹配的文件名。
## grep -w 的作用？
匹配整个单词。
## grep -A 3 "pattern" file 的作用？
显示匹配行及其后3行。
## grep -B 2 "pattern" file 的作用？
显示匹配行及其前2行。
## grep -C 1 "pattern" file 的作用？
显示匹配行及前后各1行。
## grep '^#' file 的作用？
显示以 # 开头的行。
## grep -e pattern1 -e pattern2 的作用？
匹配多个模式。
## diff 命令的作用？
比较两个文件的差异。
## diff file1 file2 的输出含义？
显示两文件间不同的行及其差异。
## diff -u 的作用？
以统一格式显示差异（常用于补丁）。
## patch 命令的作用？
根据 diff 文件修改源文件。
## cmp 命令的作用？
逐字节比较文件，显示第一个不同之处。
## comm 命令的作用？
比较两个已排序文件的不同与相同部分。
## wc 命令的作用？
统计文件的行数、单词数和字符数。
## wc -l 的作用？
统计行数。
## wc -w 的作用？
统计单词数。
## wc -c 的作用？
统计字节数。
## sort 命令的作用？
对文本文件排序。
## sort -r 的作用？
反向排序。
## sort -n 的作用？
按数值排序。
## sort -u 的作用？
去除重复行。
## uniq 命令的作用？
去除重复行（相邻行）。
## uniq -c 的作用？
统计重复次数。
## cut 命令的作用？
提取文本的列或字段。
## cut -d: -f1 /etc/passwd 的作用？
显示 /etc/passwd 文件中的第一列用户名。
## paste 命令的作用？
按列合并多个文件。
## tr 命令的作用？
替换或删除字符。
## tr 'a-z' 'A-Z' 的作用？
将小写字母转换为大写。
## tr -d '\r' 的作用？
删除回车符。
## awk 命令的作用？
按模式扫描和处理文本。
## awk '{print $1}' file 的作用？
打印文件的第一列。
## awk -F: '{print $1,$3}' /etc/passwd 的作用？
以冒号为分隔符打印用户名和UID。
## sed 命令的作用？
对文本流进行编辑（替换、删除、插入）。
## sed 's/old/new/' file 的作用？
将匹配的第一个 old 替换为 new。
## sed 's/old/new/g' file 的作用？
替换所有匹配的 old。
## sed -n '5,10p' file 的作用？
仅显示第5到10行。
## sed '/^#/d' file 的作用？
删除以 # 开头的行。
## chmod 600 file 的作用？
仅文件所有者可读写。
## chmod 700 dir 的作用？
所有者可访问该目录并执行文件。
## chown user file 的作用？
更改文件所有者。
## chgrp group file 的作用？
更改文件所属组。
## sudo 的作用？
以超级用户权限执行命令。
## sudo -l 的作用？
列出当前用户可执行的命令。
## visudo 命令的作用？
安全地编辑 /etc/sudoers 文件。
## /etc/sudoers 文件的作用？
定义哪些用户可以使用 sudo 以及权限范围。
## id 命令的作用？
显示当前用户的 UID 和 GID。
## groups 命令的作用？
显示当前用户所在的组。
## adduser 命令的作用？
添加新用户。
## passwd 命令的作用？
修改用户密码。
## deluser 命令的作用？
删除用户。
## usermod -aG group user 的作用？
将用户加入指定组。
## su 命令的作用？
切换到其他用户。
## su - user 的作用？
切换用户并加载其登录环境。
## exit 命令的作用？
退出当前 shell。
## hostname 命令的作用？
显示或修改主机名。
## uname -a 的作用？
显示操作系统内核和版本信息。
## df -h 的作用？
显示磁盘分区使用情况。
## du -sh dir 的作用？
显示目录占用的总空间。
## free -h 的作用？
显示内存使用情况。
## date 命令的作用？
显示当前日期和时间。
## cal 命令的作用？
显示日历。
## uptime 命令的作用？
显示系统运行时间。
## who 命令的作用？
显示当前登录用户。
## last 命令的作用？
显示最近登录记录。
## w 命令的作用？
显示当前登录用户及其活动。
## ps -A 的作用？
显示所有进程。
## top 的默认刷新间隔是多少秒？
3秒。
## killall -9 bash 的作用？
强制结束所有 bash 进程。
## reboot 命令的作用？
重启系统。
## shutdown -h now 的作用？
立即关机。
## shutdown -r now 的作用？
立即重启。
## halt 命令的作用？
停止系统（不切断电源）。
## poweroff 命令的作用？
关闭电源。
## systemctl poweroff 的作用？
使用 systemd 关闭系统。
## systemctl reboot 的作用？
使用 systemd 重启系统。
## systemctl isolate multi-user.target 的作用？
切换到多用户命令行模式。
## systemctl default 的作用？
切换到默认运行级别。
## systemctl list-timers 的作用？
显示系统定时任务。
## crontab -e 的作用？
编辑当前用户的定时任务。
## crontab -l 的作用？
查看当前用户的定时任务。
## crontab -r 的作用？
删除当前用户的定时任务。
## /etc/crontab 文件的作用？
系统级计划任务配置。
## at 命令的作用？
安排一次性任务。
## atq 命令的作用？
查看等待执行的任务。
## atrm 命令的作用？
删除等待的 at 任务。
## ps -eo pid,ppid,cmd 的作用？
显示进程PID、父PID与命令。
## lsof 命令的作用？
列出当前打开的文件。
## lsof -i 的作用？
显示网络连接。
## netstat -tulnp 的作用？
显示正在监听的端口。
## ping 命令的作用？
测试主机连通性。
## traceroute 命令的作用？
跟踪数据包到目标主机的路径。
## dig 命令的作用？
查询 DNS 信息。
## curl 命令的作用？
通过 URL 传输数据。
## wget 命令的作用？
从网络下载文件。
## tar -cvf file.tar dir 的作用？
将目录打包为 tar 文件。
## tar -xvf file.tar 的作用？
解包 tar 文件。
## tar -zcvf file.tar.gz dir 的作用？
压缩目录为 tar.gz 文件。
## tar -zxvf file.tar.gz 的作用？
解压 tar.gz 文件。
## gzip file 的作用？
压缩文件。
## gunzip file.gz 的作用？
解压文件。
## zip -r archive.zip dir 的作用？
压缩目录为 zip 文件。
## unzip archive.zip 的作用？
解压 zip 文件。
## file 命令的作用？
查看文件类型。
## md5sum file 的作用？
生成文件的 MD5 校验和。
## sha256sum file 的作用？
生成文件的 SHA256 校验。
## diff -q file1 file2 的作用？
仅报告文件是否不同。
## cat file | less 的作用？
分页查看文件内容。
## tail -f logfile 的作用？
实时查看日志更新。
## dmesg 命令的作用？
查看内核启动和系统信息。
## top 中按 q 的作用？
退出 top。
## top 中按 k 的作用？
终止指定进程。
## top 中按 P 的作用？
按 CPU 使用率排序。
## top 中按 M 的作用？
按内存使用率排序。
## free 命令中的 buffers/cache 表示什么？
系统缓存占用内存。
## df -Th 的作用？
显示带文件系统类型的人类可读磁盘使用情况。
## du -h --max-depth=1 的作用？
显示当前目录下各子目录的空间使用量。
## ps aux | grep root 的作用？
显示 root 用户的进程。
## lsb_release -a 的作用？
显示发行版信息。
## hostnamectl 的作用？
查看或修改主机名。
